name: bridge_software_task
description: "A simple e-comerce app"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_screenutil: ^5.9.0
  flutter_riverpod: ^2.4.9
  carousel_slider: ^4.2.1
  connectivity_plus: ^5.0.2
  dio: ^5.4.0
  flutter_spinkit: ^5.2.0
  flutter_html:
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/